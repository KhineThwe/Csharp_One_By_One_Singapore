using System;

namespace accessStrings
{
    class Access{
        public static void Main(string[] args){
         string input = "    This is a string with spaces.   ";
         string trimmed = input.Trim();
         Console.WriteLine(trimmed);
         //remove white space both beginning and end.
       }
    }
}